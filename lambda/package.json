{
  "name": "connect-multimodal-lambda",
  "version": "1.0.0",
  "description": "Lambda function for Amazon Connect integration",
  "main": "index.js",
  "scripts": {
    "test": "jest --verbose"
  },
  "dependencies": {
    "aws-sdk": "^2.1400.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
